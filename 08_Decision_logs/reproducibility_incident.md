Stage 1-4 was originally done on a lab machine under a different file path; that on migration to my machine the large artifacts (*.parquet, *.npy) did not transfer becasue they are gitignored by design; that the stage scripts carried absolute lab paths and were refactored to script-relative paths using Path(__file__).resolve().parent.parent; that Stages 3–4 were re-run deterministically (fixed embedding model, random_state=42, n_init=10, final_k=8); and that regeneration was verified byte-identical to the documented taxonomy via fc on taxonomy_table.csv.